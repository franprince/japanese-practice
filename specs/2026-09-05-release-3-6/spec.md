# Release 3.6.0

User authorization: create a release PR and update the repository description to reflect the refactors.

Package merged PRs #56–#63 from develop for review against main. Include the four-game UX, saved settings/review, session and lifecycle consolidation, Words/dataset boundaries, reliable static wordsets, hardened browser coverage, and Ollama removal. Update the GitHub About description and README to match actual behavior.

Acceptance: a release branch contains current main metadata; release notes cover user-visible changes and refactors, visual proof, validation and rollback; GitHub description is saved; release PR is conflict-free. Version/tag publication and merging remain the existing main-branch release workflow's responsibility.
